---
import { getAllTypefaces } from '../../lib/typefaces';

export async function getStaticPaths() {
  return getAllTypefaces().map(tf => ({ params: { slug: tf.slug } }));
}

return Astro.redirect('/typefaces', 301);
---
